# /recipe (조리법 LLM 편집) 엔드포인트에서만 쓰는 키입니다.
# 이 키가 없어도 /generate, /generate_day 등 나머지 기능은 전부 정상 동작합니다.
#
# 주의: 코드는 이 값을 '환경변수'에서 직접 읽습니다(.env 자동 로딩 아님).
#   Windows :  set OPENAI_API_KEY=sk-...
#   mac/linux: export OPENAI_API_KEY=sk-...
# 그 다음 같은 터미널에서 서버를 실행하세요.

OPENAI_API_KEY=sk-여기에_본인_키
