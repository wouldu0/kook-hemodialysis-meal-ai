# FOOK 통합 백엔드 실행에 필요한 파이썬 패키지 (Python 3.9 기준)
# 설치:  pip install -r requirements.txt
#
# TensorFlow는 CPU 버전으로 고정했습니다. GPU 없이도 돕니다.
# (개발 PC는 tensorflow-cpu 2.20 + tf-keras 2.20 조합이었습니다. 이 조합을 그대로 쓰세요.)

# ── API 서버 / DB ──────────────────────────────
fastapi==0.116.1
uvicorn[standard]==0.35.0
SQLAlchemy==2.0.43
psycopg[binary]==3.2.9
python-dotenv==1.1.1
pydantic[email]==2.11.7

# ── AI 생성 엔진 (app_core_FOOK, FOOK_adjust_levers) ──
tensorflow-cpu==2.20.0
tf-keras==2.20.1

numpy==2.0.2
pandas==2.3.3
scipy==1.13.1
scikit-learn==1.6.1
openpyxl==3.1.5

# util.py가 로딩 시 import하므로 필수 (생성 모델 코드가 이 안에 있음)
matplotlib==3.9.4
seaborn==0.13.2
tqdm==4.68.3

# /recipe (조리법 LLM 편집), /tts (음성 변환) 엔드포인트 전용.
# 이 기능들을 안 쓰면 없어도 서버는 뜹니다 (OPENAI_API_KEY 미설정 시 해당 엔드포인트만 오류 응답).
openai==2.45.0
